# Python backend URL (used by frontend Docker container)
PYTHON_BACKEND_URL=http://python-backend:8001

# Data directory for project storage
DATA_DIR=/data

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000
